CSS files inside this folder are copied over from `Algolia_AlgoliaSearch`
in order re-add them to the page.

From `Hyva_AlgoliaSearch` they are removed via layout XML and the action
cannot be reverted.
